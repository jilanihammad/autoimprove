"""Summary report generation — the primary deliverable of a run.

Implemented in Bead 17.
"""

from __future__ import annotations


def generate_summary() -> str:
    """Generate the final Markdown summary report.

    Sections: run summary, before/after metrics, accepted changes,
    rejected changes, criteria proposals, cost breakdown, next steps.
    """
    raise NotImplementedError("Summary report — implemented in Bead 17")
